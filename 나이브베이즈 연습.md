# 나이브베이즈 연습
### - 데이터 정리
- 데이터 불러오기
1) iris_df = pd.read_csv("/content/drive/MyDrive/dataset/iris.csv")
- 종속변수, 독립변수 나눠주기
2) x = iris_df.iloc[:, :-1] : y = iris_df.iloc[:, -1]
- 나중에 다시 데이터프레임을 만들수도 있기때문에 미리 속성이름들 빼서 리스트 만들기
3) feature_name = list(x.columns); target_name = list(y.unique())
#### * y.unique -> 겹치는거 다빼고 뽑아줘

- 넘파이 형태로 정렬
4) x=x.to_numpy() ; y = y.map({"setosa": 0, "versicolor": 1, "virginica": 2}).to_numpy()
#### * .map({}) -> 맵핑해주기

### - Gaussian 나이브 베이즈 분류 실습
import numpy as np

import pandas as pd

from sklearn.naive_bayes import CategoricalNB, GaussianNB

from sklearn.preprocessing import KBinsDiscretizer

- 데이터 셋 가져오기
1) from sklearn.datasets import load_iris
2) iris = load_iris()
#### iris 데이터는 data부분과 target부분이 나눠져있음

### - train/test 분할
1) from sklearn.model_selection import train_test_split
2) x_train, x_test, y_train, y_test = train_test_split(x,y, test_size=0.2, stratify=y)
#### * stratify = y 분할할때 원래데이터 비율과 맞춰서 분할

4) x = iris.data ; y= iris.target
- 다시 데이터 프레임을 만들기 위해 열에 들어갈 애들 만들기
4) feature_name = iris.feature_names
5) target_name = iris.feature_names
6) iris_df = pd.DataPrame(x, columns = feature_name)
- target인 y도 데이터프레임에 붙여주기
7) iris_df["species"] = y

### - 가우시안 나이브베이즈 적용
1) from sklearn.naive_bayes import CategoricalNB, GaussianNB
2) gnb.fit(x,y)
#### - gnb.class_prior_ -> 각 클래스가나올 확률
#### - gnb.theta_ -> 클래스 별 평균
#### - gnb.var_ -> 클래스 별 분산
